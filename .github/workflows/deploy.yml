name: Deploy FastAPI to VPS

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy FastAPI Application
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 158.69.113.159
          username: ubuntu
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            set -e  # Exit on error
            
            echo "═══════════════════════════════════════════════════════"
            echo "🚀 FastAPI Deployment Started"
            echo "═══════════════════════════════════════════════════════"
            echo ""
            
            # Variables
            PROJECT_DIR="/var/www/html/apielectoral/"
            APP_NAME="api-electoral"
            VENV_PATH="$PROJECT_DIR/venv"
            UVICORN_CMD="$VENV_PATH/bin/uvicorn"
            APP_MODULE="api:app"
            HOST="0.0.0.0"
            PORT="8000"
            WORKERS="2"
            
            # 1. Validar directorio del proyecto
            echo "📁 Step 1: Validating project directory..."
            if [ ! -d "$PROJECT_DIR" ]; then
              echo "❌ Error: Directory $PROJECT_DIR does not exist"
              exit 1
            fi
            cd "$PROJECT_DIR"
            echo "   ✅ Working directory: $(pwd)"
            
            # 2. Configurar Git
            echo ""
            echo "🔧 Step 2: Configuring Git..."
            if [ ! -d .git ]; then
              echo "❌ Error: Not a git repository"
              exit 1
            fi
            git config --global --add safe.directory "$PROJECT_DIR"
            echo "   ✅ Git configured"
            
            # 3. Corregir permisos
            echo ""
            echo "🔐 Step 3: Setting permissions..."
            sudo chown -R ubuntu:ubuntu "$PROJECT_DIR"
            echo "   ✅ Permissions set to ubuntu:ubuntu"
            
            # 4. Backup y actualizar código
            echo ""
            echo "📥 Step 4: Updating code from repository..."
            if [ -f .env ]; then
              cp .env .env.backup
              echo "   ✅ .env backed up"
            fi
            
            git pull origin main || git pull origin master
            echo "   ✅ Code updated"
            
            if [ -f .env.backup ]; then
              cp .env.backup .env
              echo "   ✅ .env restored"
            elif [ ! -f .env ] && [ -f .env.example ]; then
              echo "   ⚠️  .env not found, please configure it"
            fi
            
            # 5. Configurar Python y Virtual Environment
            echo ""
            echo "🐍 Step 5: Setting up Python environment..."
            
            # Verificar python3-venv
            if ! dpkg -l | grep -q python3.*-venv; then
              echo "   Installing python3-venv..."
              sudo apt update -qq
              sudo apt install -y python3-venv python3-full python3-pip
            fi
            
            # Limpiar venv corrupto
            if [ -d "$VENV_PATH" ] && [ ! -f "$VENV_PATH/bin/activate" ]; then
              echo "   Removing corrupted venv..."
              rm -rf "$VENV_PATH"
            fi
            
            # Crear venv si no existe
            if [ ! -d "$VENV_PATH" ]; then
              echo "   Creating virtual environment..."
              python3 -m venv "$VENV_PATH"
              if [ $? -ne 0 ]; then
                sudo apt update -qq
                sudo apt install -y python3-venv python3-full python3-pip
                python3 -m venv "$VENV_PATH"
              fi
            fi
            
            # Verificar venv
            if [ ! -f "$VENV_PATH/bin/activate" ]; then
              echo "❌ Failed to create virtual environment"
              exit 1
            fi
            
            # Activar venv
            source "$VENV_PATH/bin/activate"
            echo "   ✅ Virtual environment activated"
            echo "      Python: $(python --version)"
            echo "      Location: $(which python)"
            
            # 6. Instalar dependencias
            echo ""
            echo "📦 Step 6: Installing dependencies..."
            pip install --upgrade pip -q
            pip install -r requirements.txt -q
            echo "   ✅ Dependencies installed"
            
            # 7. Gestionar aplicación
            echo ""
            echo "🔄 Step 7: Managing application..."
            
            # Verificar si existe servicio systemd
            if sudo systemctl list-unit-files | grep -q "$APP_NAME.service"; then
              echo "   Using systemd service..."
              sudo systemctl restart "$APP_NAME"
              sleep 3
              if sudo systemctl is-active --quiet "$APP_NAME"; then
                echo "   ✅ Service restarted successfully"
                sudo systemctl status "$APP_NAME" --no-pager -l
              else
                echo "   ❌ Service failed to start"
                sudo journalctl -u "$APP_NAME" -n 50 --no-pager
                exit 1
              fi
            else
              echo "   ⚠️  Systemd service not found, using manual start"
              
              # Detener procesos existentes
              if pgrep -f "uvicorn.*$APP_MODULE" > /dev/null; then
                echo "   Stopping existing uvicorn process..."
                pkill -9 -f "uvicorn.*$APP_MODULE"
                sleep 2
              fi
              
              # Iniciar uvicorn
              echo "   Starting uvicorn..."
              cd "$PROJECT_DIR"
              
              # Usar setsid para crear nueva sesión y desacoplar del terminal
              setsid "$UVICORN_CMD" "$APP_MODULE" \
                --host "$HOST" \
                --port "$PORT" \
                --workers "$WORKERS" \
                --log-level info \
                > "$PROJECT_DIR/uvicorn.log" 2>&1 < /dev/null &
              
              # Desacoplar del shell
              disown
              
              # Esperar inicio
              echo "   Waiting for application to start..."
              sleep 5
              
              # Verificar proceso
              if pgrep -f "uvicorn.*$APP_MODULE" > /dev/null; then
                PID=$(pgrep -f "uvicorn.*$APP_MODULE")
                echo "   ✅ Uvicorn started (PID: $PID)"
              else
                echo "   ❌ Failed to start uvicorn"
                echo "   Last 30 lines of log:"
                tail -30 "$PROJECT_DIR/uvicorn.log" 2>/dev/null || echo "   (log not found)"
                exit 1
              fi
            fi
            
            # 8. Verificar API
            echo ""
            echo "🔍 Step 8: Testing API endpoint..."
            sleep 3
            
            if curl -f -s http://localhost:$PORT/balance > /dev/null 2>&1; then
              echo "   ✅ API is responding on port $PORT"
              RESPONSE=$(curl -s http://localhost:$PORT/balance)
              echo "   Response: $RESPONSE"
            else
              echo "   ⚠️  API not responding yet"
              echo "   This is normal, it may need more time to initialize"
            fi
            
            echo ""
            echo "═══════════════════════════════════════════════════════"
            echo "✅ Deployment completed successfully!"
            echo "═══════════════════════════════════════════════════════"

      - name: Health Check
        run: |
          sleep 10
          curl -f http://158.69.113.159:8000/balance || echo "⚠️ Health check failed or endpoint not available"

      - name: Notify Success
        if: success()
        run: |
          echo "✅ Deployment to VPS successful!"
          echo "Server: 158.69.113.159"
          echo "Timestamp: $(date)"

      - name: Notify Failure
        if: failure()
        run: |
          echo "❌ Deployment to VPS failed!"
          echo "Check logs for details"
          echo "Timestamp: $(date)"