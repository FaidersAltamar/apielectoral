name: Deploy to VPS

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to VPS Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 158.69.113.159
          username: ubuntu
          key: ${{ secrets.VPS_SSH_KEY }}
          port: 22
          script: |
            # Definir directorio del proyecto
            PROJECT_DIR="/var/www/html/apielectoral/"
            
            # Verificar que el directorio existe
            if [ ! -d "$PROJECT_DIR" ]; then
              echo "❌ Error: Directory $PROJECT_DIR does not exist"
              exit 1
            fi
            
            # Navegar al directorio del proyecto
            cd "$PROJECT_DIR"
            echo "✅ Working directory: $(pwd)"
            
            # Verificar que es un repositorio git
            if [ ! -d .git ]; then
              echo "❌ Error: Not a git repository"
              exit 1
            fi
            
            # Configurar git safe.directory para evitar error de ownership
            git config --global --add safe.directory "$PROJECT_DIR"
            echo "✅ Git safe directory configured"
            
            # Verificar y corregir permisos del directorio
            sudo chown -R ubuntu:ubuntu "$PROJECT_DIR"
            echo "✅ Directory ownership corrected"
            
            # Hacer backup del .env si existe
            if [ -f .env ]; then
              cp .env .env.backup
              echo "✅ Backed up .env file"
            else
              echo "⚠️  Warning: .env file not found"
            fi
            
            # Pull latest changes
            echo "📥 Pulling latest changes..."
            git pull origin main || git pull origin master
            
            # Restaurar .env si existe el backup
            if [ -f .env.backup ]; then
              cp .env.backup .env
              echo "✅ Restored .env file"
            fi
            
            # Limpiar venv corrupto si existe
            if [ -d venv ] && [ ! -f venv/bin/activate ]; then
              echo "🗑️  Removing corrupted venv..."
              rm -rf venv
            fi
            
            # Verificar e instalar python3-venv si es necesario
            if ! dpkg -l | grep -q python3.*-venv; then
              echo "📦 Installing python3-venv..."
              sudo apt update -qq
              sudo apt install -y python3-venv python3-full
            fi
            
            # Crear virtual environment si no existe
            if [ ! -d venv ]; then
              echo "📦 Creating virtual environment..."
              python3 -m venv venv
              if [ $? -ne 0 ]; then
                echo "❌ Failed to create venv, installing dependencies..."
                sudo apt update -qq
                sudo apt install -y python3-venv python3-full python3-pip
                python3 -m venv venv
                if [ $? -ne 0 ]; then
                  echo "❌ Still failed, trying with sudo..."
                  sudo python3 -m venv venv
                  sudo chown -R ubuntu:ubuntu venv
                fi
              fi
            fi
            
            # Verificar que venv se creó correctamente
            if [ ! -f venv/bin/activate ]; then
              echo "❌ Virtual environment activation script not found"
              echo "📋 Debugging info:"
              echo "   Python version: $(python3 --version)"
              echo "   Python location: $(which python3)"
              echo "   Venv module: $(python3 -m venv --help > /dev/null 2>&1 && echo 'OK' || echo 'NOT FOUND')"
              exit 1
            fi
            
            # Activar entorno virtual
            source venv/bin/activate
            echo "✅ Virtual environment activated"
            echo "   Python: $(which python)"
            echo "   Pip: $(which pip)"
            
            # Actualizar pip
            pip install --upgrade pip
            
            # Instalar/actualizar dependencias
            echo "📦 Installing dependencies..."
            pip install -r requirements.txt
            
            # Reiniciar el servicio si existe
            if sudo systemctl list-unit-files | grep -q "api-electoral.service"; then
              echo "🔄 Restarting service..."
              sudo systemctl restart api-electoral
              sudo systemctl status api-electoral --no-pager
            else
              echo "⚠️  Warning: api-electoral.service not found"
              echo "💡 You may need to set up the systemd service"
              
              # Intentar matar procesos existentes
              echo "🔍 Checking for existing processes..."
              if pgrep -f "python.*api.py" > /dev/null; then
                echo "   Found existing process, killing..."
                pkill -f "python.*api.py" || true
                sleep 2
              fi
              
              # Iniciar aplicación en background
              echo "🚀 Starting application in background..."
              cd "$PROJECT_DIR"
              nohup venv/bin/python api.py > app.log 2>&1 &
              APP_PID=$!
              echo "   Started with PID: $APP_PID"
              
              # Esperar a que inicie
              sleep 3
              
              # Verificar que está corriendo
              if ps -p $APP_PID > /dev/null 2>&1; then
                echo "✅ Application is running (PID: $APP_PID)"
              else
                echo "❌ Application failed to start"
                echo "📋 Last 20 lines of app.log:"
                tail -20 app.log || echo "   (log file not found)"
                exit 1
              fi
              
              # Verificar que responde
              echo "🔍 Testing API endpoint..."
              sleep 2
              if curl -s http://localhost:8000/balance > /dev/null; then
                echo "✅ API is responding"
              else
                echo "⚠️  API not responding yet (may need more time to start)"
              fi
            fi

      - name: Health Check
        run: |
          sleep 10
          curl -f http://158.69.113.159:8000/balance || echo "⚠️ Health check failed or endpoint not available"

      - name: Notify Success
        if: success()
        run: |
          echo "✅ Deployment to VPS successful!"
          echo "Server: 158.69.113.159"
          echo "Timestamp: $(date)"

      - name: Notify Failure
        if: failure()
        run: |
          echo "❌ Deployment to VPS failed!"
          echo "Check logs for details"
          echo "Timestamp: $(date)"